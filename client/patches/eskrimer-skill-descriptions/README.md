# Eskrimer skill descriptions

Updates Eskrimer client text with clean skill descriptions and buff icon metadata.

Run:

```powershell
.\Apply-EskrimerSkillDescriptionPatch.ps1 -ClientPath "C:\CloverTOS-Local"
```

Close and reopen the game client after applying the patch so the language data is reloaded.
